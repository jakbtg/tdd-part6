export function parseRLE(rle) {
  let result = [];
  // let current = "";
  // let count = 0;
  // for (let i = 0; i < rle.length; i++) {
  //   let c = rle[i];
  //   if (c === "o") {
  //     count++;
  //   } else if (c === "b") {
  //     result.push([current, count]);
  //     current = "";
  //     count = 0;
  //   } else {
  //     current += c;
  //   }
  // }
  // if (current.length > 0) {
  //   result.push([current, count]);
  // }
  return result;
}
